{
  "kind": "build_request",
  "title": "Update app title to MHT-CET College Predictor v2",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-26",
      "text": "Update the browser document title to exactly \"MHT-CET College Predictor v2\" by changing the <title> in `frontend/index.html`.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-title-change"
        ],
        "quotes": [
          "Change the title of the app to \"MHT-CET College Predictor v2\"."
        ]
      },
      "acceptanceCriteria": [
        "Opening the app shows the browser tab/title as \"MHT-CET College Predictor v2\".",
        "`frontend/index.html` contains `<title>MHT-CET College Predictor v2</title>`."
      ]
    },
    {
      "id": "REQ-27",
      "text": "Update the visible app header title text in `frontend/src/pages/MhtCetPredictorFormPage.tsx` to exactly \"MHT-CET College Predictor v2\" (currently rendered as \"MHT-CET College Predictor\").",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-title-change"
        ],
        "quotes": [
          "Change the title of the app to \"MHT-CET College Predictor v2\"."
        ]
      },
      "acceptanceCriteria": [
        "The top page header H1 in the UI displays \"MHT-CET College Predictor v2\".",
        "No other UI behavior is changed (form inputs, CSV upload, prediction submission, and results rendering continue to work as before)."
      ]
    }
  ],
  "constraints": [
    "Do not modify any files under `frontend/src/components/ui` (read-only).",
    "Do not modify immutable hook sources: `frontend/src/hooks/useInternetIdentity.ts`, `frontend/src/hooks/useInternetIdentity.tsx`, `frontend/src/hooks/useActor.ts`, `frontend/src/main.tsx`.",
    "Use English for all user-facing text."
  ],
  "nonGoals": [
    "No changes to backend Motoko canisters or any prediction/import logic.",
    "No visual/theme redesign beyond the title text updates."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}